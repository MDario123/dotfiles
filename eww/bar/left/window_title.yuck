(defpoll current_window_title :interval "1s" "hyprctl --instance 0 activewindow | rg 'title:' | awk '{ $1=\"\"; print }'")

(defwidget window_title []
  (box :class "window_title"
  (label :text current_window_title)
))
